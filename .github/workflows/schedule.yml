name: schedule crons on every 1 mins
on:
  workflow_dispatch:
  schedule:
    - cron: '* * * * *'

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "The current server time is $(date)"
