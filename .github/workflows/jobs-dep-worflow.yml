name: jobs dependent workflow
on: [push]
jobs:
    job-2:
        runs-on: ubuntu-latest
        needs: job-1
        steps:
          - name: test job need
            run: echo "Hello"
    job-1: 
        runs-on: ubuntu-latest
        steps:
          - name: test 2 need
            run: echo "world !"